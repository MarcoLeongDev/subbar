# Cargo build configuration for `subbar`.
#
# Security posture:
# - Use the system `git` binary for fetching dependencies instead of libgit2,
#   reducing the surface area of the TLS / git-fetch implementation.

[net]
git-fetch-with-cli = true